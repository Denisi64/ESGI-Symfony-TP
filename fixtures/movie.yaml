App\Entity\Movie:
  movie_{1..100}:
    title: '<word()>'
    shortDescription: '<sentence()>'
    longDescription: '<text()>'
    releaseDate: '<dateTimeBetween("-10 years", "now")>'
    coverImage: '<imageUrl()>'
    mediaType: 'movie' # Assure-toi que cette ligne est bien présente
    staff: ['<name()>', '<name()>']
    casting: ['<name()>', '<name()>']
